# This command will start the debian container
docker run -it --rm debian /bin/bash

# Requirements to compile source code
apt-get update; \
apt-get install -y clang; \
apt-get install -y git; \
apt-get install -y vim;
